## 제목 넣기
 - 배운 내용

@​Environment(\.model​Context) private var context

• Environment는 SwiftUI에서 시스템이 자동으로 제공해주는 값(예: 색상, 폰트, 화면 크기, 데이터베이스 컨텍스트 등)을 뷰에서 쉽게 쓸 수 있게 해주는 기능입니다.
• @​Environment(\.model​Context)는 SwiftDa
